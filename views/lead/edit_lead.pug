extends ../layout

block content
    form(action="/leads/" + lead.id + "/edit", method="POST")
       input(name ="lead_email", type="email", placeholder="Enter Your E-Mail Address",value = lead.email)
       button(type="submit") Save